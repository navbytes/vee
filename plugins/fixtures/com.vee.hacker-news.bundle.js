// Vee plugin bundle — com.vee.hacker-news — generated by bundle.mjs (do not edit)
"use strict";
(() => {
  // packages/sdk/src/types.ts
  var Tags = {
    root: "root",
    list: "list",
    listItem: "list-item",
    detail: "detail",
    form: "form",
    field: "field",
    action: "action",
    actionPanel: "action-panel",
    text: "text",
    empty: "empty-view"
  };

  // packages/sdk/src/dom.ts
  function node(tag, props = {}, children = []) {
    const { key, ...rest } = props;
    const n = { tag, props: rest, children };
    if (typeof key === "string") n.key = key;
    return n;
  }
  function asChildren(children) {
    if (children === void 0) return [];
    return Array.isArray(children) ? children : [children];
  }
  function root(props = {}, children) {
    return node(Tags.root, props, asChildren(children));
  }
  function list(props = {}, children) {
    return node(Tags.list, props, asChildren(children));
  }
  function listItem(props = {}, children) {
    return node(Tags.listItem, props, asChildren(children));
  }
  function action(props = {}, children) {
    return node(Tags.action, props, asChildren(children));
  }
  function actionPanel(props = {}, children) {
    return node(Tags.actionPanel, props, asChildren(children));
  }
  function empty(props = {}, children) {
    return node(Tags.empty, props, asChildren(children));
  }

  // packages/sdk/src/runtime.ts
  var PLUGIN_GLOBAL_KEY = "__veePlugin";
  var commandRegistry = /* @__PURE__ */ new Map();
  function buildRegisteredPlugin() {
    return {
      get commandNames() {
        return [...commandRegistry.keys()];
      },
      activateCommand(name, ctx) {
        const handler = commandRegistry.get(name);
        if (!handler) {
          throw new Error(`vee: no command registered named "${name}"`);
        }
        return handler(ctx);
      }
    };
  }
  function publish() {
    const reg = buildRegisteredPlugin();
    globalThis[PLUGIN_GLOBAL_KEY] = reg;
    return reg;
  }
  function definePlugin(def) {
    for (const [name, handler] of Object.entries(def.commands)) {
      commandRegistry.set(name, handler);
    }
    return publish();
  }
  function host() {
    const v = globalThis.vee;
    if (!v) {
      throw new Error(
        "vee: the host global `vee` is not present. This bundle must be evaluated inside the Vee host."
      );
    }
    return v;
  }
  function showToast(style, title, message) {
    host().showToast(style, title, message);
  }
  function http() {
    return host().http;
  }

  // samples/hacker-news/src/index.ts
  var API = "https://hacker-news.firebaseio.com/v0";
  var STORY_COUNT = 8;
  function hostOf(url) {
    if (!url) return "news.ycombinator.com";
    const m = /^[a-z]+:\/\/([^/?#]+)/i.exec(url);
    return m ? m[1].replace(/^www\./, "") : "news.ycombinator.com";
  }
  function storyItem(story) {
    const score = typeof story.score === "number" ? story.score : 0;
    const subtitle = `${score} points \xB7 ${hostOf(story.url)}`;
    const url = story.url ?? `https://news.ycombinator.com/item?id=${story.id}`;
    return listItem(
      { key: String(story.id), id: String(story.id), title: story.title ?? "(untitled)", subtitle, icon: "newspaper" },
      [
        actionPanel({}, [
          action({ actionId: "open", title: "Open in Browser", shortcut: "cmd+enter", url })
        ])
      ]
    );
  }
  function storiesTree(stories) {
    return root({}, [list({ key: "stories", filtering: true }, stories.map(storyItem))]);
  }
  function emptyTree(message) {
    return root({}, [
      list({ key: "stories", filtering: false }, [
        empty({ key: "empty", title: "No stories", description: message, icon: "wifi.slash" })
      ])
    ]);
  }
  async function parseJsonOk(res) {
    if (res.status < 200 || res.status >= 300) return void 0;
    const body = (await res.text()).trim();
    if (body.length === 0) return void 0;
    try {
      return JSON.parse(body);
    } catch {
      return void 0;
    }
  }
  async function loadTopStories(ctx) {
    try {
      const topRes = await http().fetch(`${API}/topstories.json`);
      const ids = await parseJsonOk(topRes);
      if (!Array.isArray(ids) || ids.length === 0) {
        showToast("failure", "Hacker News", "Failed to load top stories.");
        ctx.render(emptyTree("Hacker News returned no stories."));
        return;
      }
      const wanted = ids.slice(0, STORY_COUNT);
      const stories = [];
      for (const id of wanted) {
        const itemRes = await http().fetch(`${API}/item/${id}.json`);
        const story = await parseJsonOk(itemRes);
        if (story && typeof story === "object") stories.push(story);
      }
      if (stories.length === 0) {
        showToast("failure", "Hacker News", "Failed to load top stories.");
        ctx.render(emptyTree("Could not load any stories."));
        return;
      }
      ctx.render(storiesTree(stories));
    } catch (err) {
      showToast("failure", "Hacker News", "Failed to load top stories.");
      ctx.render(emptyTree(`Failed to load: ${String(err)}`));
    }
  }
  definePlugin({
    commands: {
      view: (ctx) => loadTopStories(ctx)
    }
  });
})();
//# sourceMappingURL=com.vee.hacker-news.js.map
