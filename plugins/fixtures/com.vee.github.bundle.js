// Vee plugin bundle — com.vee.github — generated by bundle.mjs (do not edit)
"use strict";
(() => {
  // packages/sdk/src/types.ts
  var Tags = {
    root: "root",
    list: "list",
    listItem: "list-item",
    detail: "detail",
    form: "form",
    field: "field",
    action: "action",
    actionPanel: "action-panel",
    text: "text",
    empty: "empty-view"
  };

  // packages/sdk/src/dom.ts
  function node(tag, props = {}, children = []) {
    const { key, ...rest } = props;
    const n = { tag, props: rest, children };
    if (typeof key === "string") n.key = key;
    return n;
  }
  function asChildren(children) {
    if (children === void 0) return [];
    return Array.isArray(children) ? children : [children];
  }
  function root(props = {}, children) {
    return node(Tags.root, props, asChildren(children));
  }
  function list(props = {}, children) {
    return node(Tags.list, props, asChildren(children));
  }
  function listItem(props = {}, children) {
    return node(Tags.listItem, props, asChildren(children));
  }
  function action(props = {}, children) {
    return node(Tags.action, props, asChildren(children));
  }
  function actionPanel(props = {}, children) {
    return node(Tags.actionPanel, props, asChildren(children));
  }
  function empty(props = {}, children) {
    return node(Tags.empty, props, asChildren(children));
  }

  // packages/sdk/src/runtime.ts
  var PLUGIN_GLOBAL_KEY = "__veePlugin";
  var commandRegistry = /* @__PURE__ */ new Map();
  function buildRegisteredPlugin() {
    return {
      get commandNames() {
        return [...commandRegistry.keys()];
      },
      activateCommand(name, ctx) {
        const handler = commandRegistry.get(name);
        if (!handler) {
          throw new Error(`vee: no command registered named "${name}"`);
        }
        return handler(ctx);
      }
    };
  }
  function publish() {
    const reg = buildRegisteredPlugin();
    globalThis[PLUGIN_GLOBAL_KEY] = reg;
    return reg;
  }
  function definePlugin(def) {
    for (const [name, handler] of Object.entries(def.commands)) {
      commandRegistry.set(name, handler);
    }
    return publish();
  }
  function host() {
    const v = globalThis.vee;
    if (!v) {
      throw new Error(
        "vee: the host global `vee` is not present. This bundle must be evaluated inside the Vee host."
      );
    }
    return v;
  }
  function getPreferenceValues() {
    return host().preferences ?? {};
  }
  function showToast(style, title, message) {
    host().showToast(style, title, message);
  }
  function http() {
    return host().http;
  }
  function open(url) {
    return host().open(url);
  }
  function onInvokeAction(handler) {
    return host().onInvokeAction(handler);
  }

  // samples/github/src/index.ts
  var SEARCH_URL = "https://api.github.com/search/issues?q=is:open+is:pr+author:@me";
  function repoOf(repositoryUrl) {
    if (!repositoryUrl) return "";
    const m = /\/repos\/([^/]+\/[^/?#]+)/.exec(repositoryUrl);
    return m ? m[1] : "";
  }
  function prSubtitle(pr) {
    const repo = repoOf(pr.repository_url);
    const state = pr.draft ? "Draft" : "Open";
    const num = `#${pr.number}`;
    return repo ? `${repo} ${num} \xB7 ${state}` : `${num} \xB7 ${state}`;
  }
  function pullRequestRow(pr) {
    return listItem(
      {
        key: String(pr.id),
        id: String(pr.id),
        title: pr.title,
        subtitle: prSubtitle(pr),
        icon: "arrow.triangle.pull"
      },
      [
        actionPanel({}, [
          action({ actionId: String(pr.id), title: "Open in Browser", shortcut: "cmd+enter" })
        ])
      ]
    );
  }
  function pullRequestsTree(prs) {
    if (prs.length === 0) {
      return root({}, [
        list({ key: "prs", filtering: false }, [
          empty({
            key: "empty",
            title: "No open pull requests",
            description: "You have no open PRs authored by you.",
            icon: "checkmark.circle"
          })
        ])
      ]);
    }
    return root({}, [list({ key: "prs", filtering: true }, prs.map(pullRequestRow))]);
  }
  function noTokenTree() {
    return root({}, [
      list({ key: "prs", filtering: false }, [
        empty({
          key: "empty",
          title: "Add a GitHub token",
          description: "Add a personal access token in Settings \u2192 Extensions \u2192 GitHub to see your pull requests.",
          icon: "key"
        })
      ])
    ]);
  }
  async function parseJsonOk(res) {
    if (res.status < 200 || res.status >= 300) return void 0;
    const body = (await res.text()).trim();
    if (body.length === 0) return void 0;
    try {
      return JSON.parse(body);
    } catch {
      return void 0;
    }
  }
  async function loadPullRequests(ctx) {
    let prs = [];
    onInvokeAction(async (p) => {
      const pr = prs.find((x) => String(x.id) === p.actionId);
      if (!pr) return;
      try {
        await open(pr.html_url);
      } catch (err) {
        showToast("failure", "Could not open PR", String(err));
      }
    });
    try {
      const { token } = getPreferenceValues();
      if (!token) {
        ctx.render(noTokenTree());
        return;
      }
      const res = await http().fetch(SEARCH_URL, {
        headers: {
          Authorization: `Bearer ${token}`,
          Accept: "application/vnd.github+json"
        }
      });
      const result = await parseJsonOk(res);
      if (!result || !Array.isArray(result.items)) {
        showToast("failure", "GitHub", "Failed to load pull requests.");
        ctx.render(pullRequestsTree([]));
        return;
      }
      prs = result.items;
      ctx.render(pullRequestsTree(prs));
    } catch (err) {
      showToast("failure", "GitHub", "Failed to load pull requests.");
      ctx.render(pullRequestsTree([]));
      void err;
    }
  }
  definePlugin({
    commands: {
      view: (ctx) => loadPullRequests(ctx)
    }
  });
})();
//# sourceMappingURL=com.vee.github.js.map
